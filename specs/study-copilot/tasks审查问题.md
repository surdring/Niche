
一、整体评价
tasks.md 整体结构清晰，覆盖了 MVP 需求，并对 Backlog 做了规划。但存在以下需要改进的问题：

二、问题清单
1. 结构规范问题
| 问题 | 说明 | 建议 | |------|------|------| | 缺少 ## Overview 标题 | 规范要求使用 ## Overview，当前使用 ## 1. 概览 | 改为 ## Overview 或保持中文但统一风格 | | 追踪矩阵格式不规范 | 规范要求 R1: T1, T2 格式，当前使用 R1 -> T5, T7, T6 | 统一为 R1: T5, T6, T7（且建议按 T# 顺序排列） |

2. 任务完整性问题
| 任务 | 问题 | 建议 | |------|------|------| | T1 | 关联需求标注为"间接支撑"，但规范要求每条任务至少引用一个 R# | 明确关联 R10（API 服务层需要工程基础）或 R11（可观测性需要日志基础） | | T20, T21 | 同样标注"间接支撑" | 明确关联 R11 | | T24 缺失 | Backlog 任务编号从 T22 跳到 T25，缺少 T24 | 补充 T24 或重新编号 |

3. Done Criteria 问题
| 任务 | 问题 | |------|------| | T33-T45 | 合并为一条，缺少独立的 Done criteria，不符合"离散可交付"原则 | | T22-T32 | 部分 Done criteria 过于简略（如 T25 "以示例插件验证"缺少具体验证标准） |

4. 依赖关系问题
| 任务 | 问题 | 建议 | |------|------|------| | T12 | 依赖 T6, T8, T9, T11，但未依赖 T10（RAGFlow Adapter） | 引用展示依赖 citations 数据，应添加 T10 依赖 | | T15 | 依赖 T3, T7, T8，但 Guardrails 应在 Agent 层执行，建议明确与 T7 的关系 | 考虑将 Guardrails 拆分为独立任务或明确在 T7 中预留接口 |

5. 需求覆盖问题
| 需求 | 问题 | |------|------| | R3 验收标准 14-17 | UI 的键盘导航、状态反馈、响应式布局、首屏加载等未在 T12 中明确体现 | | R4 验收标准 3 | TTFT 追踪在 T8 中未明确提及，仅在 T14 提到"延迟指标" | | R5 验收标准 3 | 用户取消后的"可恢复状态"在 T9 中未明确 |

6. 风险与回滚章节
当前风险识别较好，但建议补充：

T7 的复杂度风险：Agent Proxy 承载了模板解析、中间件链、结构化输出等多个职责，建议考虑是否需要拆分
T2 的契约变更风险：共享类型变更可能影响多个下游任务
三、改进建议
T33-T45 拆分：将 Research Copilot 扩展任务拆分为独立任务，每个任务有独立的产出和 Done criteria

补充 T24：建议为 R19（数据处理管道）的"OCR 支持"单独建立任务，与 T23 区分

追踪矩阵优化：

按 T# 顺序排列
使用规范格式 R#: T#, T#
Done Criteria 增强：

T12 补充：键盘可完成核心流程、响应式布局断言
T8 补充：TTFT 指标可观测
依赖关系修正：

T12 添加 T10 依赖
考虑将 T15 中的 Guardrails 部分前置到 T7 或拆分为 T7.1
四、结论
tasks.md 整体质量较高，覆盖了 MVP 核心闭环，里程碑划分合理。主要问题集中在：

格式规范（追踪矩阵格式、编号连续性）
Backlog 任务粒度过粗（T33-T45 合并）
部分 Done criteria 不够具体

_____________________________________________________________________________________


❌ 追踪矩阵部分进行详细审查发现的问题:
1. 缺失需求映射
以下需求在 requirements.md 中存在，但在追踪矩阵中完全缺失：

| 缺失需求 | 说明 | |---------|------| | R24 | 评估框架（Backlog 中有 T30，但矩阵 2.2 漏列） |

2. 追踪矩阵与任务列表不一致
| 问题 | 详情 | |------|------| | T24 缺失 | Backlog 矩阵从 T22 跳到 T25，缺少 T24 的定义 | | T33-T45 合并 | 将 R28-R40（13 个需求）合并为一个任务条目，违反"离散可交付"原则 |

3. 需求-任务映射不完整
根据 design-overview.md 的 Requirements Mapping，以下映射在 tasks.md 中缺失或不完整：

| 需求 | design-overview 指向 | tasks.md 现状 | 建议 | |------|---------------------|---------------|------| | R6 | design-contracts, design-backend, design-agent | T5, T7, T17 | ✅ 基本覆盖 | | R7 | design-agent, design-backend | T15, T19 | ⚠️ 缺少 Agent 侧 guardrails 实现任务 |

4. 反向追踪问题（T# -> R#）
部分任务的关联需求标注与矩阵不一致：

| 任务 | 任务列表中标注的 R# | 矩阵中的 R# | 问题 | |------|-------------------|-------------|------| | T1 | R9, R10（间接支撑） | 未在矩阵中出现 | 矩阵应补充或任务应移除"间接支撑"标注 | | T20 | R11（间接支撑） | 未在矩阵中出现 | 同上 | | T21 | R11 | 未在矩阵中出现 | 同上 |

📋 建议修改
我建议对 tasks.md 的追踪矩阵进行以下修正：

补充缺失的 Backlog 映射：

R24 -> T30
补充 T24 任务定义（或重新编号）

拆分 T33-T45：将 Research Copilot 扩展拆分为独立任务条目

补充间接支撑的映射（可选，但建议标注）：

R9 -> T1（间接）
R10 -> T1（间接）
R11 -> T20, T21